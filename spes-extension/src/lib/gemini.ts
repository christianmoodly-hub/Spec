/**
 * Google Gemini API client.
 * Phase 1 scaffold — not wired. Do not call from popup/options yet.
 */

export function getGeminiClient(): never {
  throw new Error('Gemini is not wired up yet (Phase 1 scaffold).')
}
